import { Controller, GET, PUT, POST, PATCH, DELETE } from "./decorators";
import startServer from "./runner";

export {
    Controller,
    GET,
    PATCH,
    POST,
    DELETE,
    PUT,
    startServer,
};